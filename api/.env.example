# Supabase JWT secret (for auth middleware)
SUPABASE_JWT_SECRET=

# CORS origin (your Vercel deployment URL)
ALLOWED_ORIGIN=

# Google credentials (optional, for service account fallback)
GOOGLE_CREDENTIALS=
